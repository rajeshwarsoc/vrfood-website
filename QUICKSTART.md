# VRfood - Quick Start Guide

## 🎯 Цель

Это руководство поможет вам быстро запустить сайт VRfood с Sanity CMS и развернуть его на Cloudflare Pages за 15-20 минут.

## ✅ Требования

- Node.js 18+ установлен
- Git установлен
- Аккаунт GitHub (бесплатный)
- Аккаунт Cloudflare (бесплатный)
- Аккаунт Sanity (бесплатный)

## 🚀 Шаг 1: Локальная установка (5 минут)

### 1.1 Клонировать и установить

```bash
# Клонировать репозиторий
git clone https://github.com/yourusername/vrfood.git
cd vrfood

# Установить зависимости
npm install
```

### 1.2 Настроить environment variables

```bash
# Скопировать пример
cp .dev.vars.example .dev.vars
```

**Временно оставьте файл пустым**, мы заполним его на следующем шаге.

## 🎨 Шаг 2: Настройка Sanity CMS (5 минут)

### 2.1 Инициализировать Sanity Studio

```bash
npm run sanity:dev
```

Это откроет браузер и предложит:
1. **Войти или создать аккаунт Sanity** (можно через Google/GitHub)
2. **Создать новый проект**
   - Название: `VRfood`
   - Dataset: `production`
3. Sanity Studio откроется локально на `http://localhost:3333`

### 2.2 Получить credentials

После создания проекта:

1. **Найти Project ID**:
   - Он отображается в адресной строке или в настройках студии
   - Или зайдите на https://www.sanity.io/manage
   - Выберите проект "VRfood"
   - Project ID показан сверху

2. **Создать API Token**:
   - В Sanity Dashboard: проект VRfood → API → Tokens
   - Нажмите "Add API token"
   - Name: `VRfood Production`
   - Permissions: выберите **Editor**
   - Скопируйте токен (важно: его нельзя будет увидеть повторно!)

### 2.3 Добавить credentials в .dev.vars

Откройте `.dev.vars` и добавьте:

```env
SANITY_PROJECT_ID=ваш-project-id-здесь
SANITY_DATASET=production
SANITY_API_TOKEN=ваш-токен-здесь
```

### 2.4 Перезапустить Sanity Studio

```bash
# Ctrl+C чтобы остановить
npm run sanity:dev
```

Теперь студия подключена к вашему проекту!

### 2.5 Добавить начальный контент

В Sanity Studio:

1. **Создать 3-5 тортов** (Торты):
   - Название, категория, описание
   - Загрузить фото
   - Установить цену
   - Отметить несколько как "Показывать на главной"

2. **Создать 4-6 блюд** (Домашняя еда):
   - Выбрать категорию (Супы, Салаты, Обеды, Ужины)
   - Добавить фото и цену
   - Отметить несколько как "Показывать на главной"

3. **Настроить Site Settings**:
   - Заголовок главной страницы
   - Подзаголовок
   - Контактные данные

## 💻 Шаг 3: Запустить локально (1 минута)

```bash
# В новом терминале (Sanity должен продолжать работать)
npm run dev
```

Откройте http://localhost:5173 - ваш сайт работает локально! 🎉

## ☁️ Шаг 4: Деплой Sanity Studio (2 минуты)

```bash
npm run sanity:deploy
```

Это создаст публичный URL для вашей студии, например:
`https://vrfood.sanity.studio`

Теперь вы сможете управлять контентом отовсюду!

## 🌐 Шаг 5: Деплой на Cloudflare Pages (5-7 минут)

### 5.1 Подготовить Git репозиторий

```bash
# Добавить remote (замените на ваш URL)
git remote add origin https://github.com/yourusername/vrfood.git

# Сделать первый commit
git add .
git commit -m "Initial VRfood setup with Sanity CMS"

# Запушить в GitHub
git branch -M main
git push -u origin main
```

### 5.2 Подключить к Cloudflare Pages

1. Зайдите на https://dash.cloudflare.com
2. Войдите или создайте бесплатный аккаунт
3. Перейдите в **Workers & Pages**
4. Нажмите **Create application** → **Pages** → **Connect to Git**
5. Авторизуйте GitHub и выберите репозиторий `vrfood`

### 5.3 Настроить сборку

В Cloudflare Pages:

- **Framework preset**: None (или React)
- **Build command**: `npm run build`
- **Build output directory**: `build/client`
- **Root directory**: (оставить пустым)

### 5.4 Добавить Environment Variables

В настройках Cloudflare Pages:

1. Перейдите в **Settings** → **Environment variables**
2. Добавьте для **Production**:
   ```
   SANITY_PROJECT_ID = ваш-project-id
   SANITY_DATASET = production
   SANITY_API_TOKEN = ваш-api-token
   ```
3. Нажмите **Save**

### 5.5 Запустить деплой

1. Нажмите **Save and Deploy**
2. Cloudflare начнет сборку (~2-3 минуты)
3. После завершения вы получите URL, например:
   `https://vrfood-abc.pages.dev`

## ✅ Готово!

Ваш сайт теперь:
- ✅ Работает локально для разработки
- ✅ Деплойнут на Cloudflare Pages
- ✅ Подключен к Sanity CMS
- ✅ Готов к приему отзывов

## 🎯 Что дальше?

### Добавить кастомный домен

1. В Cloudflare Pages: **Custom domains** → **Set up a custom domain**
2. Введите ваш домен (например, `vrfood.ru`)
3. Следуйте инструкциям по настройке DNS
4. Cloudflare автоматически настроит HTTPS

### Протестировать отзывы

1. Перейдите на `/reviews` на вашем сайте
2. Заполните форму отзыва
3. Откройте Sanity Studio
4. Найдите отзыв в разделе "Отзывы"
5. Отметьте "Одобрен" чтобы опубликовать

### Автоматические деплои

Теперь при каждом push в `main`:
```bash
git add .
git commit -m "Update content"
git push
```

Cloudflare автоматически развернет обновления (~1-2 минуты).

## 📞 Помощь

Если что-то пошло не так:

1. **Проверьте логи сборки** в Cloudflare Dashboard
2. **Проверьте .dev.vars** - корректные ли credentials
3. **Убедитесь** что API token имеет права "Editor"
4. **Смотрите** [DEPLOYMENT.md](./DEPLOYMENT.md) для детальной информации

## 🎊 Поздравляем!

Вы успешно запустили профессиональный сайт с CMS за 15-20 минут!

**Следующие шаги:**
- [ ] Добавить весь контент в Sanity
- [ ] Настроить кастомный домен
- [ ] Поделиться ссылкой с клиентами
- [ ] Начать получать отзывы!
